<div class="panel-box" style="@Style">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    // Оставляем Style на случай, если захочется поменять высоту или отступы
    [Parameter] public string Style { get; set; } = "";
}